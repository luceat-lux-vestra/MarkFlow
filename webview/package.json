{
  "name": "webview",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=26 <27"
  },
  "scripts": {
    "build": "tsc && vite build --config vite.derived-renderer.config.ts",
    "test:source": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "~7.0.2",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "katex": "^0.18.7",
    "mermaid": "12.0.0"
  },
  "overrides": {
    "mermaid": {
      "katex": "$katex"
    }
  }
}
