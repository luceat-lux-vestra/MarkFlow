# Paste boundaries

Existing   source whitespace surrounds a paste target.

Before the insertion.

```text
- clipboard text inside a code block stays literal
  and is not parsed as a list
```

After the insertion.
